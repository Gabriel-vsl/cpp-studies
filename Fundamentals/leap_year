#include <iostream>

int main() {
  int year=0;

  std::cout << "What is the year?\n";
  std::cin >> year;
  
  if (year < 1000 || year > 9999) {
    std::cout << "Invalid entry.\n";
  }

  if (year % 4 != 0) {
  std::cout << "Year " << year << " is not a leap year\n";
  }
    else if (year % 100 != 0) {
      std::cout <<"Year " << year << " is leap year\n";
    }
      else if (year % 400 == 0) {
        std::cout << "Year " << year << " is a leap year\n";
      }
  else {
    std::cout <<"Year " << year << " isn't leap year\n";
  }

/* better way
    if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
        std:: cout << year;
        std:: cout << " is a leap year\n";
    }
    else {
  std::cout << "Year ";
  std::cout << year << " is not a leap year\n";
    }

  */
    }

